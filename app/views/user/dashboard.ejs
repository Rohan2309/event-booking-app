<!DOCTYPE html>
<html lang="en">
<head>
  <title>User Dashboard – EventBook</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background: #f5f6fa;
      min-height: 100vh;
    }
    .quick-link {
      display: flex;
      align-items: center;
      justify-content: center;
      height: 120px;
      border-radius: 14px;
      background: #ffffff;
      box-shadow: 0 3px 10px rgba(0,0,0,0.08);
      font-size: 1.2rem;
      font-weight: 600;
      color: #333;
      transition: 0.2s;
      text-decoration: none;
    }
    .quick-link:hover {
      background: #4e73df;
      color: #fff;
      transform: translateY(-4px);
      text-decoration: none;
    }
  </style>
</head>

<body>

  <%- include('../partials/header') %>

  <div class="container py-4">

    <!-- FLASH MESSAGES -->
    <% if (success && success.length > 0) { %>
      <div class="alert alert-success text-center"><%= success %></div>
    <% } %>

    <% if (error && error.length > 0) { %>
      <div class="alert alert-danger text-center"><%= error %></div>
    <% } %>

    <!-- USER WELCOME -->
    <div class="mb-4">
      <h2 class="fw-bold">Welcome, <%= user.name %> 👋</h2>
      <p class="text-muted">Explore events and manage your bookings easily.</p>
    </div>

    <!-- QUICK ACTIONS -->
    <div class="row mb-4">

      <div class="col-md-4 mb-3">
        <a href="/user/events" class="quick-link">🎉 Browse Events</a>
      </div>

      <div class="col-md-4 mb-3">
        <a href="/user/bookings" class="quick-link">📄 My Bookings</a>
      </div>

      <div class="col-md-4 mb-3">
        <a href="/user/profile" class="quick-link">👤 Edit Profile</a>
      </div>

    </div>

  </div>

  <%- include('../partials/footer') %>

</body>
</html>
